Classroom Info
<%= render partial: "classroom", locals:
{classroom: @classroom} %><br>

<% @classroom.students.each do |student| %>
  <%= render partial: "students/student", locals:
  {student: student}%>
<%end%>
